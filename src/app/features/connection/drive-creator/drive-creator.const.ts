import {DriveConfig} from '../../../backend/state/base/config.abstract';


export const driveConfig: DriveConfig = {
  common: {
    color: '-',
    name: '-',
    type: '-',
  },
  workflow: {
    default: {
      isEnabled: true,
      isHidden: false,
    }
  }
};
